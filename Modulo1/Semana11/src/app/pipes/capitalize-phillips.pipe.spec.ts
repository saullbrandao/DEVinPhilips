import { CapitalizePhillipsPipe } from './capitalize-phillips.pipe';

describe('CapitalizePhillipsPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizePhillipsPipe();
    expect(pipe).toBeTruthy();
  });
});
